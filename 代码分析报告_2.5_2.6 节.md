# 代码分析报告：可解释性与多目标优化

## 📊 分析
**源文件**: `D:\Analysis1\demo3.ipynb`  
**分析范围**: 
- 2.5 节对应代码：SHAP 可解释性分析（第 804-985 行）
- 2.6 节对应代码：模型预测与评估（第 1354-1421 行、第 1500-1531 行）

---

## 🔍 任务 1: SHAP 可解释性分析代码详解

### ✅ 代码存在性确认
**位置**: 第 804-985 行（共 182 行代码）  
**库使用**: `import shap`（第 808 行）

---

### 📋 完整代码逻辑提取

#### **1. 初始化设置** (第 816-827 行)

```python
plt.rc("font", family="SimHei")

X_cols = ['功率', '扫描速度', '扫描间隙', '层厚']
y_cols = ['致密度', '阻抗 (MΩ cm-2)', 'Jcorr\n(μA cm-2)']

save_root = "active_learning/old/plots"
shap_root = os.path.join(save_root, "shap")
os.makedirs(shap_root, exist_ok=True)

# 建议选取一个小背景集，加速并稳定解释
X_bg = X_train.sample(min(100, len(X_train)), random_state=42)
```

**关键信息**:
- ✅ **特征列**: 4 个 LPBF 工艺参数（功率、扫描速度、扫描间隙、层厚）
- ✅ **目标列**: 3 个材料性能指标（致密度、阻抗、Jcorr）
- ✅ **背景数据集**: 从训练集随机采样 **100 个样本** (`random_state=42` 保证可重复性)
- ✅ **保存路径**: `active_learning/old/plots/shap/{model_name}/`

---

#### **2. 主循环结构** (第 829-834 行)

```python
for name, est in best_pipes.items():
    model_dir = os.path.join(shap_root, name)
    os.makedirs(model_dir, exist_ok=True)
    
    # 针对每个目标，构建单输出预测函数
    for idx, target in enumerate(y_cols):
```

**遍历对象**:
- **模型**: `best_pipes` 字典中的所有模型（RandomForest, XGBoost, LightGBM 等）
- **目标**: 3 个输出变量（致密度、阻抗、Jcorr）

---

#### **3. 单输出预测函数构建** (第 835-840 行)

```python
def f_pred(X):
    # X 为原始 DataFrame；管道会自动预处理
    if not isinstance(X, pd.DataFrame):
        X = pd.DataFrame(X, columns=X_train.columns)
    # est.predict 返回形状 [n_samples, n_targets]，这里取第 idx 列
    return est.predict(X)[:, idx]
```

**技术细节**:
- ✅ **输入处理**: 自动将非 DataFrame 转换为 DataFrame
- ✅ **多输出处理**: `MultiOutputRegressor` 包装的模型返回 `(n_samples, n_targets)` 形状
- ✅ **索引选择**: `[:, idx]` 提取第 `idx` 个目标的预测值
- ✅ **管道兼容**: 注释说明"管道会自动预处理"（如 StandardScaler）

---

#### **4. SHAP 解释器创建与计算** (第 843-849 行)

```python
# 使用统一的 Explainer（会根据模型类型自动选择核）
explainer = shap.Explainer(f_pred, X_bg)
shap_values = explainer(X_test)
```

**关键机制**:
- ✅ **自适应策略**: 
  - 对树模型（RF, XGB, LGBM）→ 自动使用 **Tree SHAP**（多项式时间精确计算）
  - 对其他模型 → 自动使用 **Kernel SHAP**（基于采样的近似）
- ✅ **返回值**: `shap.Explanation` 对象，包含:
  - `shap_values.values`: SHAP 值矩阵 `(n_samples, n_features)`
  - `shap_values.base_values`: 基线预测值（期望输出）
  - `shap_values.data`: 原始特征数据

---

#### **5. SHAP 值数据保存** (第 851-877 行)

```python
# 1. SHAP 值 DataFrame
shap_df = pd.DataFrame(
    shap_values.values,
    columns=[f"SHAP_{col}" for col in X_test.columns],
    index=X_test.index
)

# 2. 原始特征值 DataFrame
data_df = pd.DataFrame(
    shap_values.data,
    columns=X_test.columns,
    index=X_test.index
)

# 3. 基线预测值
base_df = pd.DataFrame(
    {"base_value": shap_values.base_values},
    index=X_test.index
)

# 4. 合并成一个总表
out_df = pd.concat([data_df, shap_df, base_df], axis=1)

# 5. 保存到 CSV
fname = f"{target}_SHAP_values.csv".replace("\n", "")
out_df.to_csv(out_path, encoding="utf-8-sig")
```

**输出内容**:
- ✅ **SHAP 值**: 每个特征的贡献度
- ✅ **原始特征值**: 用于关联分析
- ✅ **基线值**: 模型在无特征信息时的期望输出
- ✅ **文件格式**: CSV（UTF-8-SIG 编码，支持 Excel 直接打开）

---

#### **6. 四种可视化生成**

##### **(1) 条形图 - 全局特征重要性** (第 879-891 行)

```python
shap.summary_plot(
    shap_values.values,      # SHAP 值矩阵 (n_samples, n_features)
    X_test,                  # 用于显示特征取值
    feature_names=list(X_test.columns),
    plot_type="bar",         # 条形图
    show=False
)
plt.title(f"{name} - {target} 的 SHAP（bar）")
plt.savefig(..., dpi=300, bbox_inches="tight")
```

**数学原理**:
$$
\text{Importance}_i = \frac{1}{N} \sum_{n=1}^{N} |\phi_i^{(n)}|
$$

---

##### **(2) 蜜蜂图 - Beeswarm Summary Plot** (第 893-899 行)

```python
shap.summary_plot(
    shap_values.values,
    X_test,
    feature_names=list(X_test.columns),
    show=False  # 默认 beeswarm 模式
)
plt.title(f"{name} - {target} 的 SHAP（beeswarm）")
plt.savefig(...)
```

**可视化编码**:
- **纵轴**: 特征重要性排序
- **横轴**: SHAP 值（影响大小）
- **颜色**: 特征值（蓝低红高）
- **点密度**: 样本分布

---

##### **(3) 特征依赖图 - Dependence Plot** (第 901-909 行)

```python
for feat in X_cols:
    if feat in X_test.columns:
        shap.dependence_plot(
            feat,                    # 当前特征
            shap_values.values,      # SHAP 值
            X_test,                  # 特征数据
            show=False
        )
        plt.title(f"{name} - {target} - {feat} 的 SHAP 依赖图")
        plt.savefig(...)
```

**功能**:
- 绘制 `{x_i^(n), φ_i^(n)}` 散点图
- 揭示特征值与 SHAP 值的函数关系
- 自动选择交互特征进行着色

---

##### **(4) 决策图 - Decision Plot** (第 911-937 行)

```python
base_val = np.mean(shap_values.base_values)  # 平均基线值
shap_val = shap_values.values

shap.decision_plot(
    base_val,
    shap_val,               # 全部样本（注释掉的代码只显示前 50 个）
    features=X_test,
    feature_names=list(X_test.columns),
    show=False
)
plt.title(f"{name} - {target} 的 SHAP 决策图")
plt.savefig(...)
```

**数学表达**:
$$
f(\mathbf{x}) = \phi_0 + \sum_{i=1}^{M} \phi_i
$$

**用途**: 追踪单个样本从基线到最终预测的贡献路径

---

#### **7. Excel 汇总报告生成** (第 939-985 行)

```python
for name in best_pipes.keys():
    model_dir = os.path.join(shap_root, name)
    excel_path = os.path.join(model_dir, f"{name}_SHAP.xlsx")
    
    wb = load_workbook(excel_path)
    
    for target in y_cols:
        sheet_name = target.replace("\n", "_")
        ws = wb.create_sheet(sheet_name)
        
        # 插入 4 类图片
        fig_files = [
            f"{target}_SHAP_bar.png",
            f"{target}_SHAP_beeswarm.png"
        ]
        # 添加依赖图（4 个特征 × 1 个目标）
        fig_files.extend([
            f"{target}_SHAP_dep_{feat}.png"
            for feat in X_cols
        ])
        # 添加决策图
        fig_files.append(f"{target}_SHAP_decision.png")
        
        # 循环插入图片
        for fig in fig_files:
            img = XLImage(fig_path)
            img.width, img.height = 480, 320
            ws.add_image(img, f"A{row}")
            row += 20
    
    wb.save(excel_path)
```

**输出结构**:
- ✅ 每个模型一个 Excel 文件
- ✅ 每个目标一个 Sheet
- ✅ 每个 Sheet 包含 7 张图片（1 bar + 1 beeswarm + 4 dependence + 1 decision）

---

## 🔍 任务 2: 多目标优化代码分析

### ❌ 专用优化算法缺失确认

**搜索关键词**（第 1-1958 行全文检索）:
- ❌ `NSGA`
- ❌ `genetic algorithm`
- ❌ `evolutionary`
- ❌ `Pareto`
- ❌ `desirability`
- ❌ `multiobjective optimization`
- ❌ `MOPSO`
- ❌ `粒子群`
- ❌ `寻优`

**结论**: **代码中未实现任何专用的多目标优化算法**

---

### ✅ 实际实现的"优化"策略

#### **代码位置**: 第 1354-1421 行、第 1500-1531 行

#### **1. 模型结果保存** (第 1354-1408 行)

```python
results = {}
selected_models = ["RandomForest", "XGBoost", "LightGBM"]
best_pipes_filtered = {name: best_pipes[name] for name in selected_models}
best_pipes = best_pipes_filtered

for model_name, best_model in best_pipes.items():
    # 用最终模型在测试集上预测
    y_pred = best_model.predict(X_test)
    
    # ---- 整体平均指标 ----
    rmse_avg = np.sqrt(mean_squared_error(y_test, y_pred, multioutput="uniform_average"))
    mae_avg = mean_absolute_error(y_test, y_pred, multioutput="uniform_average")
    r2_avg = r2_score(y_test, y_pred, multioutput="uniform_average")
    
    # ---- 每个输出单独指标 ----
    rmse_each = np.sqrt(mean_squared_error(y_test, y_pred, multioutput="raw_values"))
    mae_each = mean_absolute_error(y_test, y_pred, multioutput="raw_values")
    r2_each = r2_score(y_test, y_pred, multioutput="raw_values")
    
    results[model_name] = {
        "Best Params": best_model.get_params(),
        "RMSE_avg": rmse_avg,
        "MAE_avg": mae_avg,
        "R2_avg": r2_avg,
        "RMSE_each": dict(zip(y_test.columns, rmse_each)),
        "MAE_each": dict(zip(y_test.columns, mae_each)),
        "R2_each": dict(zip(y_test.columns, r2_each))
    }
```

**实际操作**:
- ✅ 在**测试集**上进行预测
- ✅ 计算性能指标（R², MAE, RMSE）
- ✅ 保存模型参数和评估结果

---

#### **2. 最终训练集构建** (第 1500-1531 行)

```python
final_data = {}

for model_name in models_to_run:
    excel_path = os.path.join(save_dir, f"{model_name}_results.xlsx")
    xls = pd.ExcelFile(excel_path)
    
    # 读取所有 Round{r} 的数据（主动学习加入的样本）
    round_sheets = [s for s in xls.sheet_names 
                    if s.startswith("Round") and not s.endswith("_Perf") and not s.endswith("_Pred")]
    
    new_train_df = pd.concat([pd.read_excel(xls, sheet_name=s) for s in round_sheets], ignore_index=True)
    
    # 合并原始训练数据和新加入的数据
    orig_train_df = pd.concat([X_train, y_train], axis=1)
    full_train_df = pd.concat([orig_train_df, new_train_df], ignore_index=True)
    
    final_data[model_name] = {
        "train_df": full_train_df,   # 完整训练集（原始 + 新加入）
        "X_test": X_test.copy(),
        "y_test": y_test.copy()
    }
```

**数据流向**:
1. 从主动学习循环的 Excel 结果中读取所有轮次加入的样本
2. 与原始训练集合并，形成**最终训练集**
3. 保留原始测试集用于评估

---

#### **3. 隐式的"最优解"识别**

**代码证据**:
- 第 1423-1451 行：对 `round5.xlsx` 中的样本进行预测
- 第 1534-1633 行：在测试集上绘制真实值 vs 预测值散点图
- 第 1636-1712 行：计算残差并绘制残差图

**实际策略**:
```python
# 对 round5 中的候选样本进行预测
round5_data = pd.read_excel("round5.xlsx")
X_round5 = round5_data[feature_cols]

for model_name, model in best_pipes.items():
    y_pred = model.predict(X_round5)
    # 保存预测结果到 Excel
    predictions_df[f"{model_name}_致密度"] = y_pred[:, 0]
    predictions_df[f"{model_name}_阻抗 (MΩ cm-2)"] = y_pred[:, 1]
    predictions_df[f"{model_name}_Jcorr(μA cm-2)"] = y_pred[:, 2]
```

**方法本质**:
1. ✅ **预定义候选池**: `round5.xlsx` 文件中的工艺参数组合
2. ✅ **模型预测**: 对候选池中的所有样本进行性能预测
3. ✅ **人工筛选**: 研究者在 Excel 中手动比较预测结果，找出"最佳"参数
4. ❌ **无自动优化**: 没有算法自动搜索 Pareto 前沿或最优解

---

## 📊 代码与论文描述的对应关系

### ✅ 2.5 节（SHAP 分析）- 完美对应

| 论文章节描述 | 代码实现 | 一致性 |
|------------|---------|-------|
| 使用 SHAP 库 | `import shap` (L808) | ✅ |
| 背景数据集 100 样本 | `X_bg = X_train.sample(min(100, ...))` (L827) | ✅ |
| 三个模型（RF/XGB/LGBM） | `for name, est in best_pipes.items()` (L829) | ✅ |
| 三个目标（密度/阻抗/Jcorr） | `for idx, target in enumerate(y_cols)` (L834) | ✅ |
| 四种可视化（bar/beeswarm/dep/decision） | 第 879-937 行 | ✅ |
| Tree SHAP 自适应 | `shap.Explainers` 自动选择 (L843 注释) | ✅ |
| 保存 SHAP 值 CSV | `out_df.to_csv(...)` (L877) | ✅ |
| 汇总到 Excel | `wb.add_image(...)` (L982) | ✅ |

**评价**: 代码完全支撑 2.5 节的学术描述，可直接写入论文

---

### ⚠️ 2.6 节（多目标优化）- 需要调整表述

| 预期优化方法 | 实际代码 | 差距 |
|------------|---------|-----|
| NSGA-II / 遗传算法 | ❌ 不存在 | ❌ |
| Pareto 前沿搜索 | ❌ 不存在 | ❌ |
| Desirability function | ❌ 不存在 | ❌ |
| 连续空间搜索 | ❌ 只在离散测试集预测 | ❌ |
| **实际方法** | **测试集枚举 + 人工筛选** | ✅ |

**建议**:
1. ✅ **诚实描述**: 明确说明采用"systematic evaluation over pre-defined candidate space"
2. ✅ **强调优势**: 突出"experimental feasibility"和"avoid extrapolation"
3. ✅ **承认局限**: 说明这是保守策略，未来可集成 evolutionary algorithms
4. ❌ **避免夸大**: 不要声称使用了 NSGA-II 或 Pareto optimization

---

## 🎯 代码分析总结

### **2.5 节（SHAP 可解释性）**
- ✅ **代码完整性**: 182 行完整实现
- ✅ **技术先进性**: 使用最新 SHAP 库和自适应策略
- ✅ **可视化全面**: 4 种互补的可视化方式
- ✅ **数据可追溯**: 保存完整的 SHAP 值和原始数据
- ✅ **论文支撑度**: 100% 对应，可直接写入论文

### **2.6 节（多目标优化）**
- ❌ **专用算法缺失**: 无 NSGA-II、无 Pareto 搜索
- ✅ **替代方案**: 测试集预测 + Excel 手工比较
- ⚠️ **论文表述**: 需调整为"candidate evaluation"而非"optimization"
- 💡 **改进方向**: 可在 Future Work 中提出集成 NSGA-II

---

## 📝 写作建议

### **对于 2.5 节**:
**可以直接写**，代码完全支撑学术描述，建议强调：
1. 使用**合作博弈论**的 Shapley 值理论
2. **Tree SHAP**的多项式时间精确计算优势
3. 四种可视化的**互补性**（全局 vs 局部，个体 vs 群体）

### **对于 2.6 节**:
**需要谨慎表述**，建议措辞：
```
"The current framework adopts a conservative strategy by evaluating 
pre-defined candidate parameter combinations within the convex hull 
of training data. While not employing dedicated multi-objective 
optimization algorithms (e.g., NSGA-II), this approach ensures 
experimental feasibility and mitigates extrapolation risks."
```

---

**分析完成时间**: 2026 年 3 月 26 日  
**分析师**: 材料信息学专家 AI  
**代码版本**: demo3.ipynb (1958 行)
